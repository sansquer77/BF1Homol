"use client";
import { useEffect,useState } from "react"; import { apiRequest } from "@/lib/api/client";
type Rule=Record<string,any>;
const defaults={nome_regra:"",quantidade_fichas:15,fichas_por_piloto:15,mesma_equipe:false,descarte:false,pontos_pole:0,pontos_vr:0,pontos_posicoes:[25,18,15,12,10,8,6,4,2,1],pontos_11_colocado:25,regra_sprint:false,pontos_sprint_pole:0,pontos_sprint_vr:0,pontos_sprint_posicoes:[8,7,6,5,4,3,2,1],pontos_dobrada:false,bonus_vencedor:0,bonus_podio_completo:0,bonus_podio_qualquer:0,qtd_minima_pilotos:3,penalidade_abandono:false,pontos_penalidade:0,penalidade_auto_percent:20,pontos_campeao:150,pontos_vice:100,pontos_equipe:80};
export function RulesAdminView(){const [rules,setRules]=useState<Rule[]>([]),[form,setForm]=useState<Rule>(defaults),[error,setError]=useState("");const load=()=>apiRequest<Rule[]>("/api/v1/admin/rules").then(setRules).catch(()=>setError("Acesso restrito ao Master ou serviço indisponível."));useEffect(() => { load(); }, []);const set=(k:string,v:any)=>setForm(x=>({...x,[k]:v}));const save=async()=>{try{await apiRequest("/api/v1/admin/rules",{method:"POST",body:JSON.stringify(form)});setForm(defaults);load()}catch{setError("Não foi possível salvar a regra.")}};return <div className="admin-catalog-view"><header className="institutional-hero"><p className="eyebrow">Administração</p><h1>Regras do campeonato.</h1><p>Parâmetros centrais de apostas e pontuação, preservando o contrato legado.</p></header>{error&&<div className="calendar-state calendar-state--error">{error}</div>}<section className="panel admin-form"><h2>Nova regra</h2><div className="admin-fields"><label>Nome<input value={form.nome_regra} onChange={e=>set("nome_regra",e.target.value)}/></label><label>Fichas<input type="number" value={form.quantidade_fichas} onChange={e=>set("quantidade_fichas",Number(e.target.value))}/></label><label>Máximo por piloto<input type="number" value={form.fichas_por_piloto} onChange={e=>set("fichas_por_piloto",Number(e.target.value))}/></label><label>Mínimo de pilotos<input type="number" value={form.qtd_minima_pilotos} onChange={e=>set("qtd_minima_pilotos",Number(e.target.value))}/></label><label>Pontos P1–P20 (vírgulas)<input value={form.pontos_posicoes.join(",")} onChange={e=>set("pontos_posicoes",e.target.value.split(",").map(Number))}/></label><label>Pontos Sprint (vírgulas)<input value={form.pontos_sprint_posicoes.join(",")} onChange={e=>set("pontos_sprint_posicoes",e.target.value.split(",").map(Number))}/></label><label className="check-field"><input type="checkbox" checked={form.regra_sprint} onChange={e=>set("regra_sprint",e.target.checked)}/>Ativar regra Sprint</label><label className="check-field"><input type="checkbox" checked={form.descarte} onChange={e=>set("descarte",e.target.checked)}/>Permitir descarte</label></div><button className="primary-action" onClick={save}>Salvar regra</button></section><section className="panel admin-list"><h2>Regras cadastradas</h2><div className="table-scroll"><table><thead><tr><th>Nome</th><th>Fichas</th><th>Mínimo</th><th>Sprint</th><th>Descarte</th></tr></thead><tbody>{rules.map(r=><tr key={r.id}><td>{r.nome_regra}</td><td>{r.quantidade_fichas}</td><td>{r.qtd_minima_pilotos}</td><td>{r.regra_sprint?"Sim":"Não"}</td><td>{r.descarte?"Sim":"Não"}</td></tr>)}</tbody></table></div></section></div>}
